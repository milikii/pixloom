services:
  pixloom:
    image: pixloom:local
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pixloom
    restart: unless-stopped
    ports:
      - "7860:7860"
    volumes:
      - ./data:/data
